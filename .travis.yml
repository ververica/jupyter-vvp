language: python
services: docker
install:
  - pip install -r vvp-magics/requirements.txt
before_script: cd vvp-magics
script:
  - python -m unittest
  - python setup.py sdist
  - docker build -t vvp-magics/vvp-jupyter .